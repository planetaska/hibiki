# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Hibiki Documentation
email: planetaska@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Hibiki is a fine-grained reactivity library modeled on the signal systems in 
  Svelte 5 and SolidJS. Dependency tracking is done at runtime 
  (not by static AST analysis): while a derived value or effect is computing, 
  it sits on an observer stack, and any signal read during that window subscribes it.
baseurl: "/hibiki" # the subpath of your site, e.g. /blog
url: "https://planetaska.github.io" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: planetaska
github_username:  planetaska

# Build settings
# theme: minima
# plugins:
# - jekyll-feed
theme: jekyll-vitepress-theme
plugins:
  - jekyll-vitepress-theme

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

jekyll_vitepress:
  branding:
    site_title: Hibiki Documentation
    logo:
      default: /assets/img/logo.svg
      alt: Hibiki Logo
      width: 24
      height: 24
  syntax:
    light_theme: github
    dark_theme: github.dark
  github_star:
    enabled: true
    repository: planetaska/hibiki
    text: Star
    show_count: true

# Collections for Jekyll VitePress
collections:
  guides:
    output: true
    permalink: "/:name/"
  references:
    output: true
    permalink: "/:name/"

defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: "assets/img"
    values:
      image: true